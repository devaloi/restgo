# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=restgo
DB_PASS=restgo
DB_NAME=restgo
DB_SSLMODE=disable

# JWT
JWT_SECRET=change-me-in-production
JWT_EXPIRY=24h

# Server
SERVER_PORT=8080
REQUEST_TIMEOUT=30s

# CORS
CORS_ORIGINS=*

# Rate Limiting (requests per minute)
RATE_LIMIT=100

# Logging
LOG_LEVEL=info
